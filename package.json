{
	"name": "opencode-copilot-messages",
	"version": "0.1.0",
	"description": "OpenCode plugin for Copilot Claude via Anthropic Messages API (/v1/messages)",
	"type": "module",
	"main": "./dist/index.js",
	"types": "./dist/index.d.ts",
	"license": "MIT",
	"files": [
		"dist/",
		"README.md",
		"LICENSE"
	],
	"scripts": {
		"build": "tsc",
		"typecheck": "tsc --noEmit",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"test": "bun test",
		"prepublishOnly": "bun run build",
		"prepare": "husky"
	},
	"dependencies": {
		"@opencode-ai/plugin": "^0.15.30",
		"@ai-sdk/anthropic": "^1.0.0",
		"zod": "^3.24.0"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.3.13",
		"@types/bun": "latest",
		"husky": "^9.1.7",
		"lint-staged": "^16.2.7",
		"typescript": "^5.0.0"
	},
	"peerDependencies": {
		"typescript": "^5"
	},
	"lint-staged": {
		"*.{ts,tsx,js,jsx,json}": [
			"biome check --write",
			"biome format --write"
		]
	}
}
